pre-commit:
  commands:
    lint-staged:
      glob: "*.{ts,tsx}"
      run: pnpm eslint --fix {staged_files} && pnpm prettier --write {staged_files}
    typecheck:
      run: pnpm typecheck

pre-push:
  commands:
    tests:
      run: pnpm test
